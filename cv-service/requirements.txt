# Recco CV Service (Person A) dependencies
#
# Tested target: Python 3.10 - 3.11 (most reliable for InsightFace wheels).
# Python 3.12/3.13 can work but may need build tools for insightface.

fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
python-multipart>=0.0.9          # required by FastAPI for multipart/form uploads
pydantic>=2.5,<3.0

numpy>=1.24,<2.0
pillow>=10.0                     # robust image decoding (jpeg/png)

# Face detection + ArcFace embeddings.
insightface>=0.7.3
onnxruntime>=1.16                # CPU inference backend for insightface
